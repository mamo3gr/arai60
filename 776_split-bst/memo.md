## step 1

二分探索木の根ノード `root` と、正の整数 `K` が与えられたとき、
二分探索木を、(1) `K` 以下を含む、と、(2) `K` よりも大きいものを含むに分割する。
制約として、ノード数は `1 <= N <= 3000`.
※ `K` はシステム上では `val` になっている

まずは手作業でやってみる。

```
       7
     /   \
    5     10
   / \    / \
  3   6  8  11
 /
2
```

`K=5` のとき。
7以下なので左にいく。
5にヒットしたので、このノードの右側を、さっきの7の左にぶら下げる。
(1) の木は5と、左側。(2) の木は7を根とする。

`K=4` のとき。
7以下なので左に行く。
5以下なので左に行く。
3なので、このノードと左側を (1) とする。(2) は7を根とする。

`K=6` のとき。
7以下なので左に行く。
5よりも大きいので右に行く。
6にヒットしたので、5を根とする木を (1), 7を根とする木を (2) とする。

木の分割が難しいな。(1) と (2) の根を覚えておかないといけない？
あるいは、作成途中の (1) と (2) につなげていけばいいのかな。
(1) を small, (2) を big とする。
左に行くときは、現在のノード以下は `K` よりも大きいのだから、bigにぶら下げる。
bigは常に左の子ノードを待ち構えているように見える。番兵を使うとシンプルに書けそう。
反対に右に行くときは、現在のノード以下は `K` 以下だから、smallにぶら下げる。

処理時間とメモリ使用量を見積もる。
時間計算量は `O(N)`. 二分探索木を辿ることから。平衡なら `O(log N)` になるが、
そのような前提は見当たらなかった。Pythonの処理能力を10^7 steps/sec とすると、
3000 / 10^7 = 0.3ミリ秒のオーダーを予想する。
メモリ使用量は `O(1)`. …と、ここで、ここまで想定していたアルゴリズムが、
入力の二分探索木に含まれるノードを出力に含んでいることに気がつく。
入力の木を壊さないためには、はじめに deepcopy すれば良さそう。
その場合は、補助空間＝出力空間が `O(N)` 必要。

## step 2

ChatGPTにレビューしてもらいながらコードを整理する。

* `if node.val <= val` の条件の方が問題に沿っていて分かりやすい
* 番兵は `small_dummy` でいい
* `node` は `current` がいい
  * これには従わない

また、再帰でも書けるよ、とのことなので、それも考えてみる。

`K <= node.val` のとき、ノードの右側はそのまま使える。
`node.left` 以下を再帰的に分割して、左は左の木に、右は現在のノードの左にぶら下げる。
反対に `K > node.val` のとき、ノードの左側はそのまま使えて、
`node.right` 以下を再帰的に分割して、左は現在の右にぶら下げて、右は右の木にする。

### 他の人のコード

#### https://github.com/Satorien/LeetCode/pull/60

再帰して部下に分割させて、自分の左 or 右にぶら下げるパターン (step1,3).
LintCode版の問題では、高い方の木を返すらしい。

#### https://github.com/garunitule/coding_practice/pull/49

同様に再帰のパターン (step2).
ループでの実装もある。

>再帰なのでループでも実装できる。
>呼び出し順でスタックに積んで、popで取り出して構築すればよい。

#### https://github.com/naoto-iwase/leetcode/pull/48

LintCode版で、ヘルパー関数を再帰しつつ、左右の部分木の高さを更新する (step1).
なかなか読みづらいな…。と思っていたらstep3でツーパスに改善されていた（作る -> 高さを計る）。

### コメント集

https://docs.google.com/document/d/11HV35ADPo9QxJOpJQ24FcZvtvioli770WWdZZDaLOfg/edit?tab=t.0#heading=h.3czeid3ovy2a

https://github.com/goto-untrapped/Arai60/pull/54#discussion_r1780641914

>具体例のところが足りない気がしますね。もっと単純な例をたくさん考えています。
>一番はじめは、target を 2.5 みたいな値だと思ってエッジケースに当たらないようにして考えます。
>そうすると、切らなくてはいけないエッジは、両側のノードの値が target をまたいでいるエッジですね。そして、その後、切ったところを繋がないといけませんね。繋ぎ方が問題になるのは切ったところが4つからですかね。
>ここまで考えてから、エッジケースをちょっと考えます。全体が target よりも大きかったり小さかったり。木のサイズが0,1などの場合です。
>それから他人にやってもらうにはどうやるかを説明できるようにして、それから機械にやり方を説明します。

発想法。自分は解くまで時間がかかったので参考になる。

https://github.com/Ryotaro25/leetcode_first60/pull/50#discussion_r1912058276

再帰とループの中間形態。

## step 3

再帰の方が読みやすい。スタックの深さは最大 `O(N)` になるが、3000程度ならそこまで非常識でもない
（Pythonではデフォルトの1000を引き上げる必要があるが）。
